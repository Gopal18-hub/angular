export class OpdpendingrequestModel {
  maxid: string;
  ptnName: string;
  firstName: string;
  middleName: string;
  lastName: string;
  gender: string;
  mobile: string;
  email: string;
  passportNo: string;
  nationality: string;
  id: number;
  smsRecNo: string;
  researchFlag: number;
  modifiedPtnName: string;
  modifiedFirstName: string;
  modifiedMiddleName: string;
  modifiedLastName: string;
  uGender: string;
  uMobile: string;
  uEmail: string;
  usmsRecNo: string;
  uPassportNo: string;
  unationality: string;
  uResearchFlag: number;
  operatorName: string;
  iacode: string;
  registrationno: number;
  operatorID: number;
  approvalDoneBy: string;
  insertdatetime: string;
  foreigner: number;
  uForeigner: number;
  ssn: string;
  dateOfBirth: string;
  age: number;
  ageType: string;
  uDateOfBirth: string;
  uAge: number;
  uAgeType: string;

  //public patient: getregopapproverejectrequest[];
  constructor(
    maxid: string,
    ptnName: string,
    firstName: string,
    middleName: string,
    lastName: string,
    gender: string,
    mobile: string,
    email: string,
    passportNo: string,
    nationality: string,
    id: number,
    smsRecNo: string,
    researchFlag: number,
    modifiedPtnName: string,
    modifiedFirstName: string,
    modifiedMiddleName: string,
    modifiedLastName: string,
    uGender: string,
    uMobile: string,
    uEmail: string,
    usmsRecNo: string,
    uPassportNo: string,
    unationality: string,
    uResearchFlag: number,
    operatorName: string,
    iacode: string,
    registrationno: number,
    operatorID: number,
    approvalDoneBy: string,
    insertdatetime: string,
    foreigner: number,
    uForeigner: number,
    ssn: string,
    dateOfBirth: string,
    age: number,
    ageType: string,
    uDateOfBirth: string,
    uAge: number,
    uAgeType: string
  ) {
    (this.maxid = maxid),
      (this.ptnName = ptnName),
      (this.firstName = firstName),
      (this.middleName = middleName),
      (this.lastName = lastName),
      (this.gender = gender),
      (this.mobile = mobile),
      (this.email = email),
      (this.passportNo = passportNo),
      (this.nationality = nationality),
      (this.id = id),
      (this.smsRecNo = smsRecNo),
      (this.researchFlag = researchFlag),
      (this.modifiedPtnName = modifiedPtnName),
      (this.modifiedFirstName = modifiedFirstName),
      (this.modifiedMiddleName = modifiedMiddleName),
      (this.modifiedLastName = modifiedLastName),
      (this.uGender = uGender),
      (this.uMobile = uMobile),
      (this.uEmail = uEmail),
      (this.usmsRecNo = usmsRecNo),
      (this.uPassportNo = uPassportNo),
      (this.unationality = unationality),
      (this.uResearchFlag = uResearchFlag),
      (this.operatorName = operatorName),
      (this.iacode = iacode),
      (this.registrationno = registrationno),
      (this.operatorID = operatorID),
      (this.approvalDoneBy = approvalDoneBy),
      (this.insertdatetime = insertdatetime),
      (this.foreigner = foreigner),
      (this.uForeigner = uForeigner),
      (this.ssn = ssn),
      (this.dateOfBirth = dateOfBirth),
      (this.age = age),
      (this.ageType = ageType),
      (this.uDateOfBirth = uDateOfBirth),
      (this.uAge = uAge),
      (this.uAgeType = uAgeType);
  }
}
